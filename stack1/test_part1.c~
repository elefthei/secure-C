#include<stdio.h>
#include<string.h>

#define goodPass "Swordfish"

int main() {
  
  char passIsGood=0;
  char buf[80];

  printf("Enter password:\n");
  gets(buf);

  if(strcmp(buf,goodPass)==0)
    passIsGood=1;
  
  if (passIsGood == 1)
    printf("You win!\n");

}
